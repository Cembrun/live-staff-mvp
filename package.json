{
  "name": "live-staff-mvp",
  "version": "2.0.0",
  "description": "Live Staff Management MVP with Role-Based Authentication",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "cd server && node server.js",
    "build": "cd web && npm install && npm run build",
    "postbuild": "mkdir -p server/public && cp -r web/dist/* server/public/",
    "railway:start": "npm run build && npm start"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.2"
  },
  "engines": {
    "node": "18.x"
  }
}
